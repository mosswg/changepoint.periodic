make_cpt_class = function(data, results, period.len, dist, max.cpts, minseglen, pen.val, cost){
    # data.set=data, cpttype=dist, method=dist, 	test.stat="character",pen.type="character",pen.value="numeric",minseglen="numeric",cpts="numeric",cpts.global="numeric",cpts.periodic="numeric",ncpts.max="numeric",param.est="list",date="character",version="character"),prototype=prototype(cpttype="Not Set",date=date(),version=as(packageVersion("changepoint.periodic"),'character')))

    # return(list(sncirc.results=out,period.len=period.len,dist=dist,max.cpts=max.cpts,minseglen=minseglen,pen.val=pen.val,cost=cost))
}
